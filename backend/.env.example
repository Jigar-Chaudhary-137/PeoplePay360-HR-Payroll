# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=peoplepay360

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=peoplepay360_super_secret_jwt_key_hackathon_2026
JWT_EXPIRES_IN=7d

# Email Configuration (Optional SMTP / nodemailer)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=payroll@peoplepay360.com

# AI Assistant (Optional LLM Key - intelligent local fallback enabled if empty)
GEMINI_API_KEY=
